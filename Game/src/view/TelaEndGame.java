package view;

import javax.swing.*;
import java.awt.*;

public class TelaEndGame extends JFrame{

    public TelaEndGame() {
        setTitle("Resultado");
        setLayout(new BorderLayout());
        setSize(400, 600);
        setVisible(true);
        setLocationRelativeTo(null);
    }

}
